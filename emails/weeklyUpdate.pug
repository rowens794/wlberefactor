
include header.pug

h2.title #{competitionName} - Weekly Update

.body
  p Hi #{focusUser[0]},

  p Another week is in the books.  Keep working hard, you have until #{competitionInfo.competitionEndDate} to lose as much weight as you possibly can.
  
  .spacer

  h3.subtitle Weekly Leaderboard
  table.leaderboard
    tr
      th 
      th 
      th Weekly Loss
      th Total Loss

    each user, index in sortedUsers 
      tr.grey
        td(height=20) #{index+1}. 
        td.alignLeft(height=20) #{user.name}
        td(height=20) #{user.weeklyLoss}#{user.weeklyLoss=='N/A' ? '' : '%'}
        td(height=20) #{user.totalLoss}#{user.totalLoss=='N/A' ? '' : '%'}
  
  .spacer

  .spacer

include footer.pug

  